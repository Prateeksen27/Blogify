<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="shortcut icon" href="/imgaes/blogify.png" type="image/x-icon" style="border-radius: 50%;">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            padding-top: 60px; /* Adjust based on navbar height */
        }
        .navbar-brand {
            font-family: 'Monospace', sans-serif;
            color: white;
        }
        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.1);
        }
        .card-header {
            background-color: #343a40;
            color: white;
            border-radius: 10px 10px 0 0;
        }
        .card-body {
            padding: 20px;
        }
        .card-title {
            font-size: 1.25rem;
            margin-bottom: 15px;
        }
        .blockquote-footer {
            font-size: 0.875rem;
            color: #6c757d;
            margin-top: 15px;
        }
        .btn-primary, .btn-danger {
            margin-top: 10px;
            margin-right: 10px;
        }
        .card-header{
          display: flex;
          justify-content: space-between;
          align-items: center;
        }
        #no_blog{
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 24px;
            color: black;
            font-style: italic;
        }
    </style>
</head>
<body>
    <nav class="navbar fixed-top navbar-dark" style="background: black;">
        <div class="container">
          <a class="navbar-brand" href="/">
            BlogiFy
          </a>
        </div>
    </nav>

    <div class="container">
        <h2 class="mt-5 mb-4">New Blogs</h2>
        <a href="/new" class="btn btn-success" style="margin-bottom: 20px;">Create Blog</a>
        <% if (locals.posts && locals.posts.length > 0) { %>
            <% locals.posts.forEach(post => { %>
                <div class="card">
                    <div class="card-header">
                    <p><%= post.blogHeading %></p>    
                     <p><%= post.date %></p>   
                    </div>
                    
                    <div class="card-body">
                        <p class="card-text"><%= post.blogText %></p>
                        <footer class="blockquote-footer">By <cite title="Author"><%= post.Author %></cite></footer>
                        <a href="/edit/<%= post.id %>" class="btn btn-primary">Edit Blog</a>
                        <a href="/delete/<%= post.id %>" class="btn btn-danger">Delete Blog</a>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <p id="no_blog">No blogs yet.</p>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>
